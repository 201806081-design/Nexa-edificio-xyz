import { Typography } from '@mui/material';
import DashboardLayout from '../../layouts/DashboardLayout';

// Placeholder: se completa en los commits 4 y 5 (formulario y alta).
export default function RegistrarUnidad() {
    return (
        <DashboardLayout>
            <Typography variant="h1" sx={{ fontSize: '2rem' }}>Registrar unidad</Typography>
        </DashboardLayout>
    );
}