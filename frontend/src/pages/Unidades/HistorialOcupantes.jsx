import { Typography } from '@mui/material';
import DashboardLayout from '../../layouts/DashboardLayout';

// Placeholder: se completa en los commits 7 y 8 (historial de ocupantes).
export default function HistorialOcupantes() {
    return (
        <DashboardLayout>
            <Typography variant="h1" sx={{ fontSize: '2rem' }}>Historial de ocupantes</Typography>
        </DashboardLayout>
    );
}