import { Typography } from '@mui/material';
import DashboardLayout from '../../layouts/DashboardLayout';

// Placeholder: se completa en el commit 3 (listado con filtro por tipo).
export default function Unidades() {
    return (
        <DashboardLayout>
            <Typography variant="h1" sx={{ fontSize: '2rem' }}>Unidades del edificio</Typography>
        </DashboardLayout>
    );
}